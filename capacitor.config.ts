import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dyootify.app',
  appName: 'dyootify',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
